# Development Workflow
Research → Plan (code-architect) → TDD → Code Review → Commit

For PRs: use `git diff [base-branch]...HEAD` to see all changes across the full branch, not just the latest commit.
